<?php
/**
 * Configuration for the make-release.php script.
 */

/* Uncomment and set this if you haven't installed the Horde framework in your
 * base include path. */
//ini_set('include_path', '/horde/libs' . PATH_SEPARATOR . ini_get('include_path'));

/* Uncomment this (replacing with correct username) to set the CVSROOT
 * environment variable. */
//putenv('CVSROOT=foo@cvs.horde.org:/repository');

/* Username for horde.org.
 * Make sure you belong to the "horde" group there. */
$options['horde']['user'] = '';

/* Needed to update the Whups versions */
$options['horde']['pass'] = '';

/* Path to private SSH identity file. */
$options['ssh']['identity'] = '';

/* Value of CVS_RSH environment variable. */
$options['cvs']['cvs_rsh'] = '';

/* From: address for announcements. */
$options['ml']['from'] = '';

/* This should be set to the developer's API token */
$options['fm']['user_token'] = '';

/* Local MD5 command. */
$options['md5'] = '/usr/bin/md5sum';

/* Configuration for local MTA. */
$options['mailer']['type'] = 'sendmail';
$options['mailer']['params'] = array();
